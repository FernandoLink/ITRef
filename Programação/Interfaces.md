A partir da java 8 é possível um método ter corpo dentro das interfaces. É só declarar o método como **default**.
Não pode ter atributos, somente métodos.

Atributos - por default são public static final e podem ser omitidos.
Métodos - por default são public abstract e podem ser omitidos.

